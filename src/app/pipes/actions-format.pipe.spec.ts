import { ActionsFormatPipe } from './actions-format.pipe';

describe('ActionsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
